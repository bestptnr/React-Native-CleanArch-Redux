import { Dimensions } from "react-native"
const { width, height } = Dimensions.get('window')

const SIZES = {
    margin: 16,
    padding: 24,
    base: 8,
    width: width,
    height: height
}
export default SIZES